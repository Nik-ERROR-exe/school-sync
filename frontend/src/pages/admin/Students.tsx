import React from 'react';
import StudentRegistry from '../../features/timetable/components/StudentRegistry';

const Students = () => {
  return (
    <div className="container mx-auto">
      <StudentRegistry />
    </div>
  );
};

export default Students;