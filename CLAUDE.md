# Git Configuration Rules

- **Git Author Identity**: Always run git commands using the user's explicit configuration:
  - Name: `Nik-ERROR-exe`
  - Email: `wnikhil146@gmail.com`
- Before executing any `git commit` command, verify that `git config user.email` is set to `wnikhil146@gmail.com`.
- Never override `user.name` or `user.email` with `claude` or `noreply@anthropic.com`.
